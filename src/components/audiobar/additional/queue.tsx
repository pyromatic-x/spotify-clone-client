import IconWithTooltip from './Icon';
import { Reorder } from '@mui/icons-material';

const QueueButton = () => {
  return <IconWithTooltip tooltip="Queue" Icon={Reorder} />;
};

export default QueueButton;
